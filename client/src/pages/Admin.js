import React from "react";
import { Button, Container } from "react-bootstrap";

const Admin = () => {
    return (
        <Container>
            <Button variant={"outline-dark"} className="mt-4 p-2">Добавить Автора</Button>
        </Container>
    );
};

export default Admin;